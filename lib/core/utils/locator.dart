// import 'package:get_it/get_it.dart';


// inal GetIt locator = GetIt.instance;

// Future<void> setupLocator() async {
  // locator.registerLazySingleton<AnalyticsBloc>(() => AnalyticsBloc(analyticsRepository: );
  // locator.registerLazySingleton<AnalyticsBloc>(
  //     () => AnalyticsBloc(analyticsRepository: locator()));
  // locator.registerLazySingleton<AnalyticsRepository>(
  //     () => AnalyticsRepository(dataProvider: locator()));
  // locator.registerLazySingleton<AnalyticsDataProvider>(
  //     () => AnalyticsDataProvider(client: http.Client()));
  // locator
      // .registerLazySingletonAsync<AnalyticsDatabase>(() async => AnalyticsDatabase());
// }
